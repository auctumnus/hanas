import { AWS_BUCKET, AWS_REGION, AWS_ACCESS_ID, AWS_SECRET_ACCESS_KEY} from './constants'

export const awsSettings = {
  bucket: AWS_BUCKET,
  accessKeyId: AWS_ACCESS_ID,
  secretAccessKey: AWS_SECRET_ACCESS_KEY,
  region: AWS_REGION
}

